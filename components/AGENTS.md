# Component Rules

Prefer reuse over creating new components.

Before building a new component, check whether an existing component can be extended.

Protect consistency in spacing, hierarchy, buttons, cards, headings, and CTA rhythm.

Do not introduce page-specific styling if shared tokens or patterns already exist.

Keep components readable, modular, and easy to reuse.

Preserve mobile responsiveness.
